import 'dotenv/config';

// Real seeding (the official 7-equipment sample + synthetic operational
// data) is implemented in Phase 02 — see .ai/phases/PHASE-02-synthetic-data.md.
// This is a placeholder so `npm run seed` doesn't fail against a schema
// that no longer has the old `items` table.

console.log('Nothing to seed yet — see .ai/phases/PHASE-02-synthetic-data.md.');
